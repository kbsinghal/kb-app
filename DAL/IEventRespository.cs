using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using kb_app.Models;

namespace kb_app.DAL
{
     internal interface IEventRespository
  {
    //   List<Event> GetEvents(int amount, string sort);

    //   Event GetSingleEvent(int eventId);

      //bool InsertEvent(Event ourEvent);

      //bool DeleteEvent(int customerId);

      //bool UpdateEvent(Event ourEvent);
  }
}